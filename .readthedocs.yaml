version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - pip install sphinx_rtd_theme

python:
  install:
    - method: pip
      path: audiergon

sphinx:
  configuration: audiergon/docs/conf.py